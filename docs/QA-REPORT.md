# QA Report

日期：2026-08-22

## 自动检查

- `npm.cmd run check`：通过。
- JavaScript 语法：`app.js`、本地服务器与资源检查脚本全部通过 `node --check`。
- 本地资源：12 个必需文件存在；整琴为 `3000×989`，侧角图为 `4079×2400`，硬件细节为 `2298×2400`，琴头为 `2311×1305`，拨片裁样为 `330×380`。
- 运行载荷：约 7.5 MiB；无 MP4、WebM 或其他运行时视频。

## 浏览器检查

- 本地 Chromium：通过。
- Reduced Motion 视口：390×844、390×720、768×900、1280×720、1440×900，以及 DPR 2 的 390×844、1440×900 全部通过；高分屏 Canvas 分别为 `780×1688` 与 `2880×1800`。
- 正常动态视口：390×844 与 1440×900；封面琴身、琴颈、琴头三个镜头时刻和四章过渡均已截图。
- 交互：四章正向、逐章反向、章节导航与 Replay 全部通过；声音控件数量为 0。
- 拨片反馈：鼠标和触控均显示真实 351 拨片裁样；桌面原生鼠标图标由自定义拨片替代。
- Reduced Motion：四章短淡化流程通过。
- 控制台错误：0；页面错误：0；失败资源：0；意外远程请求：0。
- Canvas 尺寸在四个视口均与可视区域一致；每次仅有一个活动章节。
- 浏览器机器可读报告与截图保存在公开仓库之外的 `natural-resonance-work/qa-evidence`。

## 边界

- 未把参考图、原始视频或 ffmpeg 提交到公开目录。
- 页面没有分析脚本、登录、表单、购买链接或意外远程依赖。
- 产品图与拨片图均为本地运行资产；公开发布前仍需确认第三方产品图和品牌标识授权。
- 系统 Reduced Motion 设置会停用封面运镜，并把章节切换为短淡化。
